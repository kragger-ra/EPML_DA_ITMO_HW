type: xgboost
name: XGBoost
params:
  max_depth: 6
  learning_rate: 0.05
  n_estimators: 100
  subsample: 0.8
  colsample_bytree: 0.8
  gamma: 0.0
  reg_alpha: 0.0
  reg_lambda: 1.0
  random_state: ${project.seed}
  verbosity: 0
